#include "CustomQlabel.h"
#include <QPixmap>
void CustomQlabel::ImageLocal(char* path)
{
	//QSize size(2*10,2*10);
	QPixmap pimage(path);
	//bool a=pimage.save(QString(path));
	//this->setPixmap(pimage);
}